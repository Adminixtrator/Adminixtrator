# α∂мιηιχтяαтσя 

```python
import boto3 as omg                                                 # Beautiful is better than ugly.
                                                                    # Explicit is better than implicit.
import tensorflow as tf                                             # Simple is better than complex.
import tensorflow_hub as hub                                        # Complex is better than complicated.
from tensorflow import keras                                        # Flat is better than nested.
                                                                    # Sparse is better than dense.
from keras.models import Sequential                                 # Readability counts.
import tensorflow.keras.backend as K                                # Special cases aren't special enough to break the rules.
from keras.layers.recurrent import LSTM                             # Although practicality beats purity.
from keras.layers import Dense, Activation                          # Errors should never pass silently.
from keras.callbacks import LambdaCallback                          # Unless explicitly silenced.
from keras.layers.embeddings import Embedding                       # In the face of ambiguity, refuse the temptation to guess.
                                                                    # There should be one-- and preferably only one --obvious way to do it.
from sklearn.ensemble import RandomForestClassifier                 # Although that way may not be obvious at first unless you're Dutch.
from sklearn.ensemble import RandomForestRegressor                  # If the implementation is hard to explain, it's a bad idea.
from sklearn.model_selection import train_test_split                # If the implementation is easy to explain, it may be a good idea.
# Time will not permit us. lol :)                                   # Namespaces are one honking great idea -- let's do more of those!

import this
